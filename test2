def login(request):
    password = "admin123"                      # 问题1：硬编码密码
    print("login:", password)                  # 问题2：调试输出
    cmd = "rm -rf " + request.get("path", "")  # 问题3：shell=True
    subprocess.run(cmd, shell=True)
    return "ok"
